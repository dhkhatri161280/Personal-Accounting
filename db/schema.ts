import { integer, real, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const accounts=sqliteTable("accounts",{id:integer("id").primaryKey({autoIncrement:true}),tallyGuid:text("tally_guid").unique(),name:text("name").notNull(),parentName:text("parent_name"),category:text("category").notNull(),currency:text("currency").notNull().default("USD"),openingBalance:real("opening_balance").notNull().default(0),isHistorical:integer("is_historical",{mode:"boolean"}).notNull().default(true)});
export const vouchers=sqliteTable("vouchers",{id:integer("id").primaryKey({autoIncrement:true}),tallyGuid:text("tally_guid").notNull(),voucherNumber:text("voucher_number"),voucherType:text("voucher_type").notNull(),transactionDate:text("transaction_date").notNull(),narration:text("narration"),currency:text("currency").notNull().default("USD"),isHistorical:integer("is_historical",{mode:"boolean"}).notNull().default(true),importedAt:text("imported_at").notNull()},t=>[uniqueIndex("vouchers_tally_guid_idx").on(t.tallyGuid)]);
export const entries=sqliteTable("entries",{id:integer("id").primaryKey({autoIncrement:true}),voucherId:integer("voucher_id").notNull().references(()=>vouchers.id),accountId:integer("account_id").notNull().references(()=>accounts.id),amount:real("amount").notNull(),isDebit:integer("is_debit",{mode:"boolean"}).notNull()});
export const migrationRuns=sqliteTable("migration_runs",{id:integer("id").primaryKey({autoIncrement:true}),sourceLabel:text("source_label").notNull(),startedAt:text("started_at").notNull(),completedAt:text("completed_at"),voucherCount:integer("voucher_count").notNull().default(0),accountCount:integer("account_count").notNull().default(0),status:text("status").notNull(),checksum:text("checksum")});