"use client";import {useState} from "react";import {useRouter} from "next/navigation";
export function TransactionForm({accounts}:{accounts:Array<{id:number;name:string;category:string}>}){const router=useRouter();const [status,setStatus]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus("Saving…");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/transactions",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await r.json();if(!r.ok){setStatus(j.error||"Unable to save");return}setStatus("Saved successfully");router.push("/transactions");router.refresh()}return <form className="entry-form" onSubmit={submit}><label>Transaction type<select name="type" required><option>Payment</option><option>Receipt</option><option>Contra</option><option>Journal</option></select></label><label>Date<input name="date" type="date" required defaultValue={new Date().toISOString().slice(0,10)}/></label><label>Amount (USD)<input name="amount" type="number" min="0.01" step="0.01" required/></label><label>Debit account<select name="debitAccountId" required><option value="">Select account</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name} — {a.category}</option>)}</select></label><label>Credit account<select name="creditAccountId" required><option value="">Select account</option>{accounts.map(a=><option key={a.id} value={a.id}>{a.name} — {a.category}</option>)}</select></label><label>Reference<input name="reference"/></label><label className="wide">Narration<textarea name="narration" rows={4}/></label><div className="wide form-actions"><button type="submit" className="primary">Save balanced transaction</button><span>{status}</span></div></form>}