"use client";
import {useEffect,useMemo,useRef,useState} from "react";
import {TransactionTable} from "@/components/TransactionTable";
import {MastersPanel,type MasterGroup} from "@/components/MastersPanel";
import {validateVoucher} from "@/lib/voucher-validation";
type Entry={accountId:number;accountName:string;amount:number};type Tx={id:number;guid:string;tallyGuid?:string;syncFingerprint?:string;syncStatus?:string;lastSyncedAt?:string;date:string;number:string;type:string;narration:string;historical:boolean;cancelled?:boolean;deleted?:boolean;entries:Entry[]};type Account={id:number;name:string;parent:string;category:string;currency:string;openingBalance:number;active?:boolean;masterSyncStatus?:"pending"|"synced";masterOriginalName?:string;tallyGuid?:string;tallyMasterId?:number;masterFingerprint?:string;masterDeletePending?:boolean};type Ledger={version:number;company:string;currency:string;createdAt:string;accounts:Account[];transactions:Tx[];groups?:MasterGroup[];currencies?:string[];voucherTypes?:string[];fiscalYearStartMonth?:number};type Vault={version:number;iterations:number;salt:string;iv:string;tag:string;ciphertext:string};
const bytes=(s:string)=>Uint8Array.from(atob(s),c=>c.charCodeAt(0));const b64=(b:ArrayBuffer|Uint8Array)=>{const a=b instanceof Uint8Array?b:new Uint8Array(b);let s="";for(let i=0;i<a.length;i+=32768)s+=String.fromCharCode(...a.subarray(i,i+32768));return btoa(s)};
function cleanText(value:string){return(value||"").replace(/&amp;/gi,"&").replace(/&apos;|&#39;|&#x27;/gi,String.fromCharCode(39)).replace(/&quot;|&#34;|&#x22;/gi,String.fromCharCode(34)).replace(/&lt;/gi,"<").replace(/&gt;/gi,">")}
function isDebitNatureAccount(account:{parent?:string;category?:string;name?:string}){const parent=cleanText(account.parent||"").toLowerCase(),category=cleanText(account.category||"").toLowerCase(),name=cleanText(account.name||"").toLowerCase(),classification=`${parent} ${category}`;if(name.includes("credit card"))return false;if(/capital|liabilit|sundry creditors|payable/.test(classification))return false;return /asset|bank accounts|cash-in-hand|cash|deposit|investment|fixed assets|loans & advances \(asset\)|ppf/.test(`${classification} ${name}`)}
function displayLedgerBalance(account:{parent?:string;category?:string;name?:string},value:number){return isDebitNatureAccount(account)?Math.abs(value||0):(value||0)}
async function contentEtag(raw:string){const hash=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(raw));return `"${Array.from(new Uint8Array(hash)).map(b=>b.toString(16).padStart(2,"0")).join("")}"`}
async function keyFor(password:string,salt:Uint8Array,it:number){const m=await crypto.subtle.importKey("raw",new TextEncoder().encode(password),"PBKDF2",false,["deriveKey"]);return crypto.subtle.deriveKey({name:"PBKDF2",salt,iterations:it,hash:"SHA-256"},m,{name:"AES-GCM",length:256},false,["encrypt","decrypt"])}
async function decryptVault(v:Vault,password:string){const key=await keyFor(password,bytes(v.salt),v.iterations),cipher=bytes(v.ciphertext),tag=bytes(v.tag),joined=new Uint8Array(cipher.length+tag.length);joined.set(cipher);joined.set(tag,cipher.length);const zipped=await crypto.subtle.decrypt({name:"AES-GCM",iv:bytes(v.iv)},key,joined);const stream=new Blob([zipped]).stream().pipeThrough(new DecompressionStream("gzip"));return JSON.parse(await new Response(stream).text()) as Ledger}
async function encryptVault(data:Ledger,password:string){const iterations=600000,salt=crypto.getRandomValues(new Uint8Array(16)),iv=crypto.getRandomValues(new Uint8Array(12)),key=await keyFor(password,salt,iterations),stream=new Blob([JSON.stringify(data)]).stream().pipeThrough(new CompressionStream("gzip")),zipped=await new Response(stream).arrayBuffer(),sealed=new Uint8Array(await crypto.subtle.encrypt({name:"AES-GCM",iv},key,zipped));return {version:1,algorithm:"AES-256-GCM",kdf:"PBKDF2-SHA256",iterations,salt:b64(salt),iv:b64(iv),tag:b64(sealed.slice(-16)),ciphertext:b64(sealed.slice(0,-16))}}
const BIO_KEY="personal-ledger-biometric-v1";const url64=(b:ArrayBuffer|Uint8Array)=>{const a=b instanceof Uint8Array?b:new Uint8Array(b);return btoa(String.fromCharCode(...a)).replace(/\+/g,"-").replace(/\//g,"_").replace(/=+$/g,"")};const fromUrl64=(s:string)=>Uint8Array.from(atob(s.replace(/-/g,"+").replace(/_/g,"/").padEnd(Math.ceil(s.length/4)*4,"=")),c=>c.charCodeAt(0));const aesKey=(secret:ArrayBuffer)=>crypto.subtle.importKey("raw",secret,{name:"AES-GCM"},false,["encrypt","decrypt"]);const fiscalYearOf=(date:string)=>{const y=Number(date.slice(0,4)),m=Number(date.slice(5,7));return m>=4?y:y-1};const nextVoucherNumber=(data:Ledger,type:string,date:string,excludeGuid?:string)=>{const fy=fiscalYearOf(date),wanted=type.toLowerCase(),numbers=data.transactions.filter(t=>!t.deleted&&!t.cancelled&&t.guid!==excludeGuid&&t.type.toLowerCase()===wanted&&fiscalYearOf(t.date)===fy).map(t=>Number(t.number)).filter(Number.isFinite);return String((numbers.length?Math.max(...numbers):0)+1)};function repairLocalDuplicateNumbers(data:Ledger){const seen=new Set<string>(),max=new Map<string,number>(),ordered=[...data.transactions].sort((a,b)=>Number(b.historical)-Number(a.historical));let changed=false;for(const t of ordered){if(t.deleted||t.cancelled)continue;const group=`${t.type.toLowerCase()}|${fiscalYearOf(t.date)}`,n=Number(t.number);if(Number.isFinite(n))max.set(group,Math.max(max.get(group)||0,n));const key=`${group}|${t.number}`;if(!t.historical&&seen.has(key)){const next=(max.get(group)||0)+1;max.set(group,next);t.number=String(next);changed=true}seen.add(`${group}|${t.number}`)}return changed}
export function VaultApp({book="us"}:{book?:"us"|"india"}){const apiUrl=book==="india"?"/api/vault?book=india":"/api/vault",biometricKey=`${BIO_KEY}-${book}`,sharedBiometricKey=`${BIO_KEY}-shared`,sessionKey=`personal-ledger-session-${book}`,sharedSessionKey="personal-ledger-shared-session",biometricSessionKey="personal-ledger-biometric-session",unifiedSecretKey="fintech-unified-prf-secret",unifiedSealKey="fintech-unified-vault-seal";const autoBiometricStarted=useRef(false),entryFormRef=useRef<HTMLFormElement>(null);const [password,setPassword]=useState(""),[data,setData]=useState<Ledger|null>(null),[status,setStatus]=useState(""),[lastSynced,setLastSynced]=useState(""),[hasBiometric,setHasBiometric]=useState(false),[biometricChecked,setBiometricChecked]=useState(false),[showPasswordFallback,setShowPasswordFallback]=useState(false),[tab,setTab]=useState("dashboard"),[report,setReport]=useState("trial"),[year,setYear]=useState("all"),[customStart,setCustomStart]=useState("2026-04"),[customEnd,setCustomEnd]=useState("2026-06"),[query,setQuery]=useState(""),[tableFilter,setTableFilter]=useState(""),[minAmount,setMinAmount]=useState(""),[sortKey,setSortKey]=useState("name"),[sortDir,setSortDir]=useState<"asc"|"desc">("asc"),[copyTx,setCopyTx]=useState<Tx|null>(null),[editTx,setEditTx]=useState<Tx|null>(null),[dashboardDetail,setDashboardDetail]=useState<"cash"|"investments"|"capital"|"salary"|"active"|"period"|null>(null),[cashFlowDetail,setCashFlowDetail]=useState<string|null>(null),[selected,setSelected]=useState<number|null>(null),[selectedVoucher,setSelectedVoucher]=useState<Tx|null>(null),[inlineLedgerSide,setInlineLedgerSide]=useState<"debit"|"credit"|null>(null),[vaultEtag,setVaultEtag]=useState("");
async function cacheUnifiedVaultPassword(pw:string){const secret=sessionStorage.getItem(unifiedSecretKey);if(!secret)return;const key=await aesKey(fromUrl64(secret)),iv=crypto.getRandomValues(new Uint8Array(12)),sealed=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,new TextEncoder().encode(pw));localStorage.setItem(unifiedSealKey,JSON.stringify({iv:url64(iv),sealed:url64(sealed)}))}async function unifiedVaultPassword(){const secret=sessionStorage.getItem(unifiedSecretKey),raw=localStorage.getItem(unifiedSealKey);if(!secret||!raw)throw Error();const config=JSON.parse(raw),key=await aesKey(fromUrl64(secret)),plain=await crypto.subtle.decrypt({name:"AES-GCM",iv:fromUrl64(config.iv)},key,fromUrl64(config.sealed));return new TextDecoder().decode(plain)}async function openVault(pw:string){const response=await fetch(apiUrl,{cache:"no-store"});if(!response.ok)throw Error();const raw=await response.text(),etag=await contentEtag(raw),v=JSON.parse(raw),decrypted=await decryptVault(v,pw),repaired=repairLocalDuplicateNumbers(decrypted);setVaultEtag(etag);setPassword(pw);setData(decrypted);const latestFiscalYear=decrypted.transactions.filter(t=>!t.deleted&&/^\d{4}-\d{2}-\d{2}$/.test(t.date)).reduce((latest,t)=>Math.max(latest,fiscalYearOf(t.date)),0);if(latestFiscalYear)setYear(String(latestFiscalYear));sessionStorage.setItem(sessionKey,pw);await cacheUnifiedVaultPassword(pw).catch(()=>{});setLastSynced(new Date().toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"}));if(repaired){setStatus("Correcting duplicate voucher number and saving securely...");const corrected=await encryptVault(decrypted,pw),saved=await fetch(apiUrl,{method:"PUT",headers:{"Content-Type":"application/json","If-Match":etag},body:JSON.stringify(corrected)});if(!saved.ok)throw Error();const repairedSave=await saved.json().catch(()=>null);if(repairedSave?.etag)setVaultEtag(repairedSave.etag);setStatus("Duplicate voucher number corrected with the next fiscal-year running number.")}else setStatus("")}async function unlock(e:React.FormEvent){e.preventDefault();setStatus("Decrypting locally...");try{await openVault(password)}catch{setStatus("Unable to unlock. Check the vault password.")}}
async function enableBiometric(){try{if(!window.PublicKeyCredential)throw Error("WebAuthn is unavailable");setStatus("Waiting for device biometric confirmation...");const salt=crypto.getRandomValues(new Uint8Array(32)),challenge=crypto.getRandomValues(new Uint8Array(32)),userId=crypto.getRandomValues(new Uint8Array(32)),credential=await navigator.credentials.create({publicKey:{challenge,rp:{name:"FinTech by DK",id:location.hostname},user:{id:userId,name:"personal-ledger-owner",displayName:"FinTech by DK Owner"},pubKeyCredParams:[{alg:-7,type:"public-key"},{alg:-257,type:"public-key"}],authenticatorSelection:{authenticatorAttachment:"platform",residentKey:"required",userVerification:"required"},timeout:60000,attestation:"none",extensions:{prf:{eval:{first:salt}}} as any}}) as PublicKeyCredential|null;if(!credential)throw Error("Biometric setup was cancelled");let secret=(credential.getClientExtensionResults() as any)?.prf?.results?.first as ArrayBuffer|undefined;if(!secret){const assertion=await navigator.credentials.get({publicKey:{challenge:crypto.getRandomValues(new Uint8Array(32)),allowCredentials:[{id:credential.rawId,type:"public-key"}],userVerification:"required",timeout:60000,extensions:{prf:{eval:{first:salt}}} as any}}) as PublicKeyCredential|null;secret=(assertion?.getClientExtensionResults() as any)?.prf?.results?.first}if(!secret)throw Error("This biometric provider does not support secure PRF unlock");const key=await aesKey(secret),iv=crypto.getRandomValues(new Uint8Array(12)),sealed=await crypto.subtle.encrypt({name:"AES-GCM",iv},key,new TextEncoder().encode(password));const biometricConfig=JSON.stringify({credentialId:url64(credential.rawId),salt:url64(salt),iv:url64(iv),sealed:url64(sealed)});localStorage.setItem(sharedBiometricKey,biometricConfig);localStorage.setItem(biometricKey,biometricConfig);setHasBiometric(true);setStatus("Biometric unlock enabled on this device.")}catch(e){setStatus(e instanceof Error?e.message:"Biometric setup failed. Password unlock remains available.")}}
async function biometricUnlock(){try{setStatus("Confirm your identity on this device...");const stored=localStorage.getItem(sharedBiometricKey)||localStorage.getItem(biometricKey)||localStorage.getItem(`${BIO_KEY}-${book==="us"?"india":"us"}`),config=JSON.parse(stored||"null");if(stored&&!localStorage.getItem(sharedBiometricKey))localStorage.setItem(sharedBiometricKey,stored);if(!config)throw Error("Biometric unlock is not configured on this device");const assertion=await navigator.credentials.get({publicKey:{challenge:crypto.getRandomValues(new Uint8Array(32)),allowCredentials:[{id:fromUrl64(config.credentialId),type:"public-key"}],userVerification:"required",timeout:60000,extensions:{prf:{eval:{first:fromUrl64(config.salt)}}} as any}}) as PublicKeyCredential|null,secret=(assertion?.getClientExtensionResults() as any)?.prf?.results?.first as ArrayBuffer|undefined;if(!secret)throw Error("Secure biometric key was not returned");const key=await aesKey(secret),plain=await crypto.subtle.decrypt({name:"AES-GCM",iv:fromUrl64(config.iv)},key,fromUrl64(config.sealed)),pw=new TextDecoder().decode(plain);sessionStorage.setItem(sharedSessionKey,pw);sessionStorage.setItem("personal-ledger-session-us",pw);sessionStorage.setItem("personal-ledger-session-india",pw);sessionStorage.setItem(biometricSessionKey,"1");await openVault(pw)}catch{setStatus("Biometric unlock failed. Use your vault password instead.")}}
function removeBiometric(){localStorage.removeItem(sharedBiometricKey);localStorage.removeItem(`${BIO_KEY}-us`);localStorage.removeItem(`${BIO_KEY}-india`);setHasBiometric(false);setShowPasswordFallback(true);setStatus("Biometric unlock removed from this device.")}async function lockVault(){try{await fetch("/_auth/logout",{method:"POST"})}catch{}sessionStorage.removeItem("personal-ledger-session-us");sessionStorage.removeItem("personal-ledger-session-india");sessionStorage.removeItem(sharedSessionKey);sessionStorage.removeItem(biometricSessionKey);sessionStorage.removeItem(unifiedSecretKey);setData(null);setPassword("")}async function save(next:Ledger,destination="daybook"){setStatus("Encrypting and saving...");const vault=await encryptVault(next,password),r=await fetch(apiUrl,{method:"PUT",headers:{"Content-Type":"application/json",...(vaultEtag?{"If-Match":vaultEtag}:{})},body:JSON.stringify(vault)});if(r.status===412){await syncNow(true);setStatus("Newer synchronized data was loaded. Your form values are retained; review and save again.");return false}if(!r.ok){setStatus("Save failed");return false}const saved=await r.json().catch(()=>null);if(saved?.etag)setVaultEtag(saved.etag);setData(next);setLastSynced(new Date().toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"}));setStatus("Encrypted save complete");setTab(destination);return true}
async function syncNow(silent=false){if(!data)return;try{if(!silent)setStatus("Syncing encrypted vault...");const response=await fetch(`${apiUrl}${apiUrl.includes("?")?"&":"?"}sync=${Date.now()}`,{cache:"no-store"});if(!response.ok)throw Error();const raw=await response.text(),etag=await contentEtag(raw),v=JSON.parse(raw),latest=await decryptVault(v,password);setVaultEtag(etag);setData(latest);setLastSynced(new Date().toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"}));if(!silent)setStatus("Cloud sync complete")}catch{if(!silent)setStatus("Unable to sync. Your current screen remains unchanged.")}}useEffect(()=>{const unifiedReady=!!sessionStorage.getItem(unifiedSecretKey)&&!!localStorage.getItem(unifiedSealKey);if(unifiedReady){setHasBiometric(true);setBiometricChecked(true);setShowPasswordFallback(false);void unifiedVaultPassword().then(openVault).catch(()=>{localStorage.removeItem(unifiedSealKey);setShowPasswordFallback(true)});return}const stored=localStorage.getItem(sharedBiometricKey)||localStorage.getItem(biometricKey)||localStorage.getItem(`${BIO_KEY}-${book==="us"?"india":"us"}`),configured=!!stored;if(stored&&!localStorage.getItem(sharedBiometricKey))localStorage.setItem(sharedBiometricKey,stored);setHasBiometric(configured);setBiometricChecked(true);setShowPasswordFallback(!configured);const cached=sessionStorage.getItem(sessionKey)||sessionStorage.getItem(sharedSessionKey),biometricSession=sessionStorage.getItem(biometricSessionKey)==="1";if(configured&&(!cached||!biometricSession)&&!autoBiometricStarted.current){autoBiometricStarted.current=true;void biometricUnlock()}else if(cached){openVault(cached).catch(()=>{sessionStorage.removeItem(sessionKey);sessionStorage.removeItem(sharedSessionKey);sessionStorage.removeItem(biometricSessionKey)})}},[]);useEffect(()=>{if(!data)return;let checking=false;const check=async()=>{if(checking||document.visibilityState!=="visible")return;checking=true;try{const response=await fetch(`${apiUrl}${apiUrl.includes("?")?"&":"?"}revision=${Date.now()}`,{method:"HEAD",cache:"no-store"}),remote=response.headers.get("ETag");if(response.ok&&remote&&remote!==vaultEtag)await syncNow(true)}catch{}finally{checking=false}},timer=setInterval(check,30000),visible=()=>{if(document.visibilityState==="visible")check()};document.addEventListener("visibilitychange",visible);return()=>{clearInterval(timer);document.removeEventListener("visibilitychange",visible)}},[data,password,vaultEtag]);
const calc=useMemo(()=>{const empty={opening:new Map<number,number>(),debit:new Map<number,number>(),credit:new Map<number,number>(),closing:new Map<number,number>(),period:[] as Tx[]};if(!data)return empty;const start=year==="all"?"0000-00-00":year==="custom"?`${customStart}-01`:year.length===7?`${year}-01`:`${year}-04-01`,end=year==="all"?"9999-99-99":year==="custom"?`${customEnd}-31`:year.length===7?`${year}-31`:`${Number(year)+1}-03-31`,opening=new Map(data.accounts.map(a=>[a.id,a.openingBalance])),debit=new Map<number,number>(),credit=new Map<number,number>(),period:Tx[]=[];for(const t of data.transactions){if(t.deleted)continue;if(t.cancelled){if(t.date>=start&&t.date<=end)period.push(t);continue}if(t.date<start){for(const e of t.entries)opening.set(e.accountId,(opening.get(e.accountId)||0)+e.amount)}else if(t.date<=end){period.push(t);for(const e of t.entries){if(e.amount<0)debit.set(e.accountId,(debit.get(e.accountId)||0)-e.amount);else credit.set(e.accountId,(credit.get(e.accountId)||0)+e.amount)}}}const closing=new Map<number,number>();for(const a of data.accounts)closing.set(a.id,(opening.get(a.id)||0)-(debit.get(a.id)||0)+(credit.get(a.id)||0));return{opening,debit,credit,closing,period}},[data,year,customStart,customEnd]);
if(!data)return <div className="unlock"><div className="vault-mark">DK</div><h1>Unlock FinTech by DK</h1>{!biometricChecked?<><p>Preparing secure device authentication...</p></>:hasBiometric&&!showPasswordFallback?<><p>Use your device biometric to unlock both US and India Books.</p><button className="biometric-primary" onClick={biometricUnlock}>Unlock with fingerprint, face, or Windows Hello</button><button className="password-fallback" onClick={()=>setShowPasswordFallback(true)}>Use vault password instead</button></>:<><p>Your vault password is processed only in this browser.</p><form onSubmit={unlock}><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Vault password" required autoFocus/><button className="primary">Unlock vault</button></form>{hasBiometric&&<button className="password-fallback" onClick={()=>setShowPasswordFallback(false)}>Back to biometric unlock</button>}</>}<small>{status}</small></div>;
const fmt=(n:number)=>new Intl.NumberFormat(data.currency==="INR"?"en-IN":"en-US",{style:"currency",currency:data.currency||"USD"}).format(n),tol=.005,years=[...new Set(data.transactions.filter(t=>!t.deleted).map(t=>{const y=Number(t.date.slice(0,4)),m=Number(t.date.slice(5,7));return String(m>=4?y:y-1)}))].sort().reverse(),months=[...new Set(data.transactions.filter(t=>!t.deleted).map(t=>t.date.slice(0,7)))].sort().reverse();const rows=data.accounts.map(a=>({...a,opening:calc.opening.get(a.id)||0,debit:calc.debit.get(a.id)||0,credit:calc.credit.get(a.id)||0,closing:calc.closing.get(a.id)||0})),active=rows.filter(a=>Math.abs(a.opening)>tol||a.debit>tol||a.credit>tol||Math.abs(a.closing)>tol),debits=active.reduce((s,a)=>s+(a.closing<0?-a.closing:0),0),credits=active.reduce((s,a)=>s+(a.closing>0?a.closing:0),0);const isCashBank=(a:Account)=>/^(bank accounts|cash-in-hand)$/.test((a.parent||"").toLowerCase());const cashBank=rows.filter(isCashBank).reduce((s,a)=>s-a.closing,0),cashIds=new Set(rows.filter(isCashBank).map(a=>a.id)),cashOpening=-rows.filter(isCashBank).reduce((s,a)=>s+a.opening,0),cashFlowItems=calc.period.filter(t=>!t.cancelled).map(t=>{const movement=-t.entries.filter(e=>cashIds.has(e.accountId)).reduce((s,e)=>s+e.amount,0),counter=t.entries.filter(e=>!cashIds.has(e.accountId)),groups=[...new Set(counter.map(e=>data.accounts.find(a=>a.id===e.accountId)?.parent||"Other"))];return{t,movement,group:groups.join(" / ")||"Cash and bank transfer"}}).filter(x=>Math.abs(x.movement)>tol),cashFlowGroups=[...cashFlowItems.reduce((m,x)=>{const v=m.get(x.group)||{group:x.group,inflow:0,outflow:0};if(x.movement>0)v.inflow+=x.movement;else v.outflow+=-x.movement;m.set(x.group,v);return m},new Map<string,{group:string;inflow:number;outflow:number}>()).values()].sort((a,b)=>a.group.localeCompare(b.group)),cashInflows=cashFlowGroups.reduce((s,g)=>s+g.inflow,0),cashOutflows=cashFlowGroups.reduce((s,g)=>s+g.outflow,0),cashNet=cashInflows-cashOutflows,cashFlowClosing=cashOpening+cashNet,investmentRows=rows.filter(a=>/^investments$/i.test(a.parent||"")&&Math.abs(a.closing)>tol),investments=investmentRows.reduce((s,a)=>s-a.closing,0);const compact=(label:string,items:typeof rows,pattern:RegExp)=>({label,value:items.filter(a=>pattern.test(a.name)).reduce((s,a)=>s-a.closing,0)}),cashHighlights=(book==="us"?[compact("AMEX",rows,/^AMEX Credit Card$/i),compact("BofA",rows,/^Bank Of America$/i),compact("Citi",rows,/^Citi Credit Card$/i)]:[compact("Axis",rows,/^Axis Bank$/i),compact("HDFC",rows,/^HDFC Bank - Hiral$/i),compact("PPF",rows,/^SBI\s*-\s*PPF Account$/i)]).filter(x=>Math.abs(x.value)>tol),investmentHighlights=(book==="us"?[compact("401K",investmentRows,/^401K Investments$/i),compact("ESPP",investmentRows,/^ESPP Deduction$/i),compact("Canyon",investmentRows,/Canyon/i)]:[compact("LIC",investmentRows,/^LIC\b/i),compact("Max",investmentRows,/^Max\b/i)]).filter(x=>Math.abs(x.value)>tol);const masterGroups=new Map((data.groups||[]).map(g=>[g.name.toLowerCase(),g])),natureFor=(a:typeof rows[number])=>{const group=(a.parent||"").toLowerCase(),configured=masterGroups.get(group);if(group.includes("(asset)"))return "Asset";if(configured)return configured.nature;if(/^(direct incomes|indirect incomes|sales accounts)$/.test(group))return "Income";if(/^(direct expenses|indirect expenses|purchase accounts)$/.test(group))return "Expense";if(/^(capital account|reserves & surplus)$/.test(group))return "Capital";if(/^(current liabilities|loans \(liability\)|bank od a\/c|secured loans|unsecured loans|duties & taxes|provisions|sundry creditors)$/.test(group))return "Liability";if(group==="bank accounts")return "Bank";if(group==="cash-in-hand")return "Cash";if(group==="investments")return "Investment";return "Asset";};const isProfitLoss=(a:typeof rows[number])=>/profit\s*&\s*loss|income\s*&\s*expenditure/i.test(a.name),parentOf=(a:typeof rows[number])=>(a.parent||"").toLowerCase(),isIncome=(a:typeof rows[number])=>!isProfitLoss(a)&&natureFor(a)==="Income",isExpense=(a:typeof rows[number])=>!isProfitLoss(a)&&natureFor(a)==="Expense",isLiability=(a:typeof rows[number])=>!isProfitLoss(a)&&["Liability","Capital"].includes(natureFor(a)),isAsset=(a:typeof rows[number])=>!isProfitLoss(a)&&["Asset","Bank","Cash","Investment"].includes(natureFor(a));const incomeRows=active.filter(isIncome),expenseRows=active.filter(isExpense),periodIncomeRows=incomeRows.map(a=>({...a,closing:a.credit-a.debit})).filter(a=>Math.abs(a.closing)>tol),periodExpenseRows=expenseRows.map(a=>({...a,closing:a.debit-a.credit})).filter(a=>Math.abs(a.closing)>tol),assetRows=active.filter(a=>isAsset(a)&&Math.abs(a.closing)>tol),liabilityRows=active.filter(a=>isLiability(a)&&Math.abs(a.closing)>tol),periodIncome=periodIncomeRows.reduce((s,a)=>s+a.closing,0),periodExpense=periodExpenseRows.reduce((s,a)=>s+a.closing,0),periodSurplus=periodIncome-periodExpense;const latestDate=data.transactions.filter(t=>!t.deleted).reduce((m,t)=>t.date>m?t.date:m,"0000-00-00"),balanceEnd=year==="all"?latestDate:year==="custom"?`${customEnd}-31`:year.length===7?`${year}-31`:`${Number(year)+1}-03-31`,balanceFY=fiscalYearOf(balanceEnd),balanceFYStart=`${balanceFY}-04-01`,nominalIds=new Set(rows.filter(a=>isIncome(a)||isExpense(a)).map(a=>a.id));let capitalTransfer=0;for(const t of data.transactions)if(!t.deleted&&t.date>=balanceFYStart&&t.date<=balanceEnd)for(const e of t.entries)if(nominalIds.has(e.accountId))capitalTransfer+=e.amount;const filtered=calc.period.filter(t=>!query||`${t.date} ${t.number} ${t.type} ${t.narration} ${t.entries.map(e=>e.accountName).join(" ")}`.toLowerCase().includes(query.toLowerCase())).slice().reverse().slice(0,750);const selectedRow=rows.find(a=>a.id===selected),selectedTx=selected?calc.period.filter(t=>t.entries.some(e=>e.accountId===selected)).slice().reverse():[];
async function createLedgerInsideVoucher(e:React.FormEvent<HTMLFormElement>){e.preventDefault();if(!inlineLedgerSide)return;const form=new FormData(e.currentTarget),name=String(form.get("name")||"").trim().replace(/\s+/g," "),parent=String(form.get("parent")||""),currency=String(form.get("currency")||data.currency),amount=Math.abs(Number(form.get("opening")||0)),side=String(form.get("side")||"Dr");if(!name||!parent){setStatus("Enter a ledger name and select its account group.");return}if(data.accounts.some(a=>a.name.toLowerCase()===name.toLowerCase())){setStatus(`Ledger ${name} already exists. Select it from the voucher instead.`);return}const nature=(data.groups||[]).find(g=>g.name===parent)?.nature||(/^bank accounts$/i.test(parent)?"Bank":/^cash-in-hand$/i.test(parent)?"Cash":/^investments$/i.test(parent)?"Investment":/income/i.test(parent)?"Income":/expense|purchase/i.test(parent)?"Expense":/capital/i.test(parent)?"Capital":/liabilit|creditor|loan/i.test(parent)?"Liability":"Asset"),id=Math.max(0,...data.accounts.map(a=>a.id))+1,account:Account={id,name,parent,category:nature,currency,openingBalance:side==="Dr"?-amount:amount,active:true,masterSyncStatus:"pending"},next={...data,accounts:[...data.accounts,account]};if(!await save(next,"new"))return;setInlineLedgerSide(null);setStatus(`Created ledger ${name}. It is selected for this voucher and pending Tally synchronization.`);requestAnimationFrame(()=>{const select=entryFormRef.current?.elements.namedItem(inlineLedgerSide) as HTMLSelectElement|null;if(select)select.value=String(id)})}function editVoucher(t:Tx){if(t.cancelled){setStatus("Cancelled vouchers are audit-only and cannot be edited.");return}if(t.entries.length!==2||!t.entries.some(e=>e.amount<0)||!t.entries.some(e=>e.amount>0)){setStatus("Only two-ledger balanced vouchers can currently be edited.");return}setEditTx(t);setCopyTx(null);setSelected(null);setTab("new");setStatus("Editing existing voucher. Review changes carefully before updating.")}function copyVoucher(t:Tx){if(t.entries.length!==2||!t.entries.some(e=>e.amount<0)||!t.entries.some(e=>e.amount>0)){setStatus("Only two-ledger balanced vouchers can currently be copied.");return}setCopyTx(t);setEditTx(null);setSelected(null);setTab("new");setStatus("Voucher copied. Change any field, then save as a new voucher.")}function deleteVoucher(t:Tx){if(!confirm(`Permanently delete ${t.type} voucher ${t.number} from both the app and Tally?`))return;if(!t.tallyGuid&&!t.syncFingerprint){save({...data,transactions:data.transactions.filter(x=>x.guid!==t.guid)});setStatus(`Voucher ${t.number} deleted.`);return}const deleted={...t,deleted:true,syncStatus:"pending"};save({...data,transactions:data.transactions.map(x=>x.guid===t.guid?deleted:x)});setStatus(`Voucher ${t.number} removed from the app. It will be deleted from Tally automatically.`)}async function add(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),amount=Number(f.get("amount")),debit=Number(f.get("debit")),credit=Number(f.get("credit"));if(!amount||debit===credit){setStatus("Enter an amount and choose two different accounts.");return}const byId=new Map(data.accounts.map(a=>[a.id,a.name])),tx:Tx={id:editTx?.id||data.transactions.length+1,guid:editTx?.guid||crypto.randomUUID(),...(editTx?{tallyGuid:editTx.tallyGuid,syncFingerprint:editTx.syncFingerprint,syncStatus:editTx.syncStatus,lastSyncedAt:editTx.lastSyncedAt}:{}),date:String(f.get("date")),number:editTx&&editTx.type===String(f.get("type"))&&fiscalYearOf(editTx.date)===fiscalYearOf(String(f.get("date")))?editTx.number:nextVoucherNumber(data,String(f.get("type")),String(f.get("date")),editTx?.guid),type:String(f.get("type")),narration:String(f.get("narration")||""),historical:editTx?.historical||false,cancelled:editTx?.cancelled||false,entries:[{accountId:debit,accountName:byId.get(debit)||"",amount:-amount},{accountId:credit,accountName:byId.get(credit)||"",amount}]};const validation=validateVoucher(tx,data.accounts);if(!validation.valid){setStatus(validation.errors.join(" "));return}const nextTransactions=editTx?data.transactions.map(t=>t.guid===editTx.guid?tx:t):[...data.transactions,tx];if(await save({...data,transactions:nextTransactions})){setCopyTx(null);setEditTx(null)}}
const capitalRows=active.filter(a=>parentOf(a)==="capital account"&&(Math.abs(a.opening)>tol||Math.abs(a.closing)>tol)),capitalClosing=capitalRows.reduce((s,a)=>s+a.closing,0),capitalOpening=capitalRows.reduce((s,a)=>s+a.opening,0),dashboardCapitalBase=year==="all"?capitalOpening:capitalClosing,dashboardCapitalResult=year==="all"?periodSurplus:(year==="custom"||year.length===7)?periodSurplus:capitalTransfer,dashboardCapitalTotal=dashboardCapitalBase+dashboardCapitalResult,capitalHighlights=[{label:"Capital",value:dashboardCapitalBase},{label:"RE",value:dashboardCapitalResult}];const salaryDetail=rows.filter(a=>/salary/i.test(a.name)).map(a=>({...a,closing:a.credit-a.debit})).filter(a=>Math.abs(a.closing)>tol),salaryIncome=salaryDetail.reduce((s,a)=>s+a.closing,0),salaryHighlights=salaryDetail.slice().sort((a,b)=>b.closing-a.closing).slice(0,3).map(a=>({label:a.name.replace(/^salary(?: income)?\s*-\s*/i,"")||a.name,value:a.closing}));const filteredActive=active.filter(a=>!tableFilter||`${a.name} ${a.parent||a.category}`.toLowerCase().includes(tableFilter.toLowerCase())).filter(a=>!minAmount||Math.max(Math.abs(a.opening),a.debit,a.credit,Math.abs(a.closing))>=Number(minAmount)).sort((a,b)=>{const av=sortKey==="name"?a.name.toLowerCase():sortKey==="group"?(a.parent||a.category).toLowerCase():Number(a[sortKey as "opening"|"debit"|"credit"|"closing"]||0),bv=sortKey==="name"?b.name.toLowerCase():sortKey==="group"?(b.parent||b.category).toLowerCase():Number(b[sortKey as "opening"|"debit"|"credit"|"closing"]||0),result=typeof av==="string"?av.localeCompare(String(bv)):av-Number(bv);return sortDir==="asc"?result:-result});const sortBy=(key:string)=>{if(sortKey===key)setSortDir(sortDir==="asc"?"desc":"asc");else{setSortKey(key);setSortDir("asc")}};const SortHead=({field,children,right=false}:{field:string;children:React.ReactNode;right?:boolean})=><th className={`${right?"right ":""}sortable`}><button onClick={()=>sortBy(field)}>{children}<span className="sort-mark" data-direction={sortKey===field?sortDir:"none"} aria-hidden="true"/></button></th>;const TableFilters=()=> <div className="table-filters"><label>Filter ledger or group<input value={tableFilter} onChange={e=>setTableFilter(e.target.value)} placeholder="Type to filter..."/></label><label>Minimum absolute value<input type="number" min="0" step="0.01" value={minAmount} onChange={e=>setMinAmount(e.target.value)} placeholder="Any amount"/></label><button onClick={()=>{setTableFilter("");setMinAmount("");setSortKey("name");setSortDir("asc")}}>Clear</button><span>{filteredActive.length} of {active.length} ledgers</span></div>;const periodLabel=year==="all"?"All periods":year==="custom"?`${new Date(`${customStart}-01T00:00:00`).toLocaleDateString("en-US",{month:"short",year:"numeric"})} to ${new Date(`${customEnd}-01T00:00:00`).toLocaleDateString("en-US",{month:"short",year:"numeric"})}`:year.length===7?new Date(`${year}-01T00:00:00`).toLocaleDateString("en-US",{month:"long",year:"numeric"}):`FY ${year} (Apr ${year} - Mar ${Number(year)+1})`;const LedgerLink=({a}:{a:typeof rows[number]})=><button className="ledger-link" onClick={()=>setSelected(a.id)}>{a.name}</button>;
const startNewVoucher=()=>{setEditTx(null);setCopyTx(null);setSelected(null);setSelectedVoucher(null);setTab("new");setStatus("")};const toggleDashboardDetail=(kind:"cash"|"investments"|"capital"|"salary"|"active"|"period")=>setDashboardDetail(current=>current===kind?null:kind);const DashboardInline=({kind}:{kind:"cash"|"investments"|"capital"|"salary"|"active"|"period"})=>{if(dashboardDetail!==kind)return null;const titles={cash:"Cash and Bank Balances",investments:"Investment Ledgers",capital:"Capital Account Composition",salary:`Salary Income - ${periodLabel}`,active:"Active Ledgers",period:`Period Vouchers - ${periodLabel}`};const detailRows=kind==="cash"?rows.filter(a=>isCashBank(a)&&Math.abs(a.closing)>tol):kind==="investments"?active.filter(a=>/^investments$/i.test(a.parent||"")&&Math.abs(a.closing)>tol):kind==="capital"?capitalRows:kind==="salary"?salaryDetail:kind==="active"?[...active].sort((a,b)=>Math.abs(b.closing)-Math.abs(a.closing)):[];return <div className="dashboard-inline-detail"><div className="dashboard-inline-heading"><strong>{titles[kind]}</strong><small>Tap card again to close</small></div>{kind==="period"?calc.period.slice().reverse().slice(0,20).map(t=><button type="button" className="dashboard-inline-row dashboard-inline-button" key={t.guid} onClick={()=>setSelectedVoucher(t)}><span>{t.date} Â· {t.type} {t.number}<small>{t.narration}</small></span><b>{fmt(Math.max(...t.entries.map(e=>Math.abs(e.amount)),0))}</b></button>):<>{detailRows.slice(0,20).map(a=><button type="button" className="dashboard-inline-row dashboard-inline-button" key={a.id} onClick={()=>setSelected(a.id)}><span>{a.name}<small>{a.parent||a.category}</small></span><b>{fmt(kind==="cash"||kind==="investments"?-a.closing:a.closing)}</b></button>)}{kind==="capital"&&<button type="button" className="dashboard-inline-row dashboard-inline-button current-result" onClick={()=>{setDashboardDetail(null);setReport("income");setTab("reports")}}><span>{year==="custom"||year.length===7?"Selected-period":"Current fiscal-year"} surplus / (deficit)</span><b>{fmt(dashboardCapitalResult)}</b></button>}</>}</div>};return <><header><div><small>FINTECH BY DK - ACCOUNTING RELEASE 5</small><div className="book-heading"><h1>Dignesh Khatri</h1><span className={`book-badge ${book}`}>{book==="india"?"INDIA BOOKS | INR":"US BOOKS | USD"}</span></div><p>{active.length} active ledgers | {calc.period.length} vouchers in selected period</p></div><div className="header-actions">{hasBiometric?<button onClick={removeBiometric}>Remove biometric</button>:<button onClick={enableBiometric}>Enable biometric</button>}<button onClick={lockVault}>Lock</button></div></header><div className="app-nav"><button className={tab==="dashboard"?"selected":""} onClick={()=>setTab("dashboard")}>Dashboard</button><button className={tab==="daybook"?"selected":""} onClick={()=>setTab("daybook")}>Day Book</button><button className={tab==="ledgers"?"selected":""} onClick={()=>setTab("ledgers")}>Ledgers</button><button className={tab==="masters"?"selected":""} onClick={()=>setTab("masters")}>Masters</button><button className={tab==="reports"?"selected":""} onClick={()=>setTab("reports")}>Reports</button><button type="button" className={`new-voucher-nav ${tab==="new"?"selected":""}`} onClick={startNewVoucher}>New Voucher</button></div><div className="period-bar"><strong>Financial period</strong><select value={year} onChange={e=>setYear(e.target.value)}><option value="all">All periods</option><option value="custom">Custom month range</option><optgroup label="Fiscal years (April to March)">{years.map(y=><option value={y} key={y}>FY {y} (Apr {y} - Mar {Number(y)+1})</option>)}</optgroup><optgroup label="Month and year">{months.map(m=><option value={m} key={m}>{new Date(`${m}-01T00:00:00`).toLocaleDateString("en-US",{month:"long",year:"numeric"})}</option>)}</optgroup></select>{year==="custom"&&<div className="custom-range"><label>From<input type="month" value={customStart} max={customEnd} onChange={e=>setCustomStart(e.target.value)}/></label><span>to</span><label>To<input type="month" value={customEnd} min={customStart} onChange={e=>setCustomEnd(e.target.value)}/></label></div>}<span>Opening + period activity = closing</span></div>{status&&<div className="vault-status">{status}</div>}
{tab==="dashboard"&&<section className="stats dashboard-stats"><div className="dashboard-card-slot cash-slot"><button className="dashboard-balance-card cash-card" onClick={()=>toggleDashboardDetail("cash")}><div className="dashboard-card-main"><span>Cash and bank closing</span><strong>{fmt(cashBank)}</strong><small>View account breakdown</small></div><div className="dashboard-card-highlights">{cashHighlights.map(x=><span key={x.label}><b>{x.label}</b><em>{fmt(x.value)}</em></span>)}</div></button><DashboardInline kind="cash"/></div><div className="dashboard-card-slot investment-slot"><button className="dashboard-balance-card investment-card" onClick={()=>toggleDashboardDetail("investments")}><div className="dashboard-card-main"><span>Investments closing</span><strong>{fmt(investments)}</strong><small>View investment ledgers</small></div><div className="dashboard-card-highlights">{investmentHighlights.map(x=><span key={x.label}><b>{x.label}</b><em>{fmt(x.value)}</em></span>)}</div></button><DashboardInline kind="investments"/></div><div className="dashboard-card-slot capital-slot"><button className="dashboard-balance-card capital-card" onClick={()=>toggleDashboardDetail("capital")}><div className="dashboard-card-main"><span>Capital closing</span><strong>{fmt(dashboardCapitalTotal)}</strong><small>Includes current result</small></div><div className="dashboard-card-highlights capital-highlights">{capitalHighlights.map(x=><span key={x.label}><b>{x.label}</b><em>{fmt(x.value)}</em></span>)}</div></button><DashboardInline kind="capital"/></div><div className="dashboard-card-slot salary-slot"><button className="dashboard-balance-card salary-card" onClick={()=>toggleDashboardDetail("salary")}><div className="dashboard-card-main"><span>Salary income</span><strong>{fmt(salaryIncome)}</strong><small>{periodLabel}</small></div><div className="dashboard-card-highlights salary-highlights">{salaryHighlights.map(x=><span key={x.label}><b title={x.label}>{x.label}</b><em>{fmt(x.value)}</em></span>)}</div></button><DashboardInline kind="salary"/></div><div className="dashboard-card-slot active-slot"><button className="dashboard-card-active" onClick={()=>toggleDashboardDetail("active")}><span>Active ledgers</span><strong>{active.length}</strong><small>View active ledger list</small></button><DashboardInline kind="active"/></div><div className="dashboard-card-slot period-slot"><button className="dashboard-card-period" onClick={()=>toggleDashboardDetail("period")}><span>Period vouchers</span><strong>{calc.period.length}</strong><small>View Day Book - {periodLabel}</small></button><DashboardInline kind="period"/></div></section>}
{tab==="daybook"&&<div className="data-panel"><input className="search-box" placeholder="Search all voucher content" value={query} onChange={e=>setQuery(e.target.value)}/><TransactionTable transactions={filtered} formatAmount={fmt} onView={t=>setSelectedVoucher(t as Tx)} onEdit={t=>editVoucher(t as Tx)} onCopy={t=>copyVoucher(t as Tx)} onDelete={t=>deleteVoucher(t as Tx)}/></div>}
{tab==="ledgers"&&<div className="data-panel"><TableFilters/><table><thead><tr><SortHead field="name">Ledger</SortHead><SortHead field="group">Group</SortHead><SortHead field="opening" right>Opening</SortHead><SortHead field="debit" right>Period Dr</SortHead><SortHead field="credit" right>Period Cr</SortHead><SortHead field="closing" right>Closing</SortHead></tr></thead><tbody>{filteredActive.map(a=><tr key={a.id}><td><LedgerLink a={a}/></td><td>{a.parent||a.category}</td><td className="right">{fmt(a.opening)}</td><td className="right">{a.debit?fmt(a.debit):"-"}</td><td className="right">{a.credit?fmt(a.credit):"-"}</td><td className="right">{fmt(a.closing)}</td></tr>)}</tbody></table></div>}
{tab==="masters"&&<div className="data-panel"><MastersPanel data={data} onSave={(next,message)=>{setStatus(message);void save(next as Ledger,"masters")}}/></div>}
{tab==="reports"&&<><div className="report-picker"><button className={report==="trial"?"selected":""} onClick={()=>setReport("trial")}>Trial Balance</button><button className={report==="income"?"selected":""} onClick={()=>setReport("income")}>Income & Expenditure</button><button className={report==="balance"?"selected":""} onClick={()=>setReport("balance")}>Balance Sheet</button><button className={report==="cash"?"selected":""} onClick={()=>setReport("cash")}>Cash and Bank</button><button className={report==="cashflow"?"selected":""} onClick={()=>setReport("cashflow")}>Cash Flow</button></div>{report==="trial"&&<div className="data-panel"><h3>Trial Balance - {periodLabel}</h3><TableFilters/>{(()=>{const tR=filteredActive.map(a=>{const nom=(isIncome(a)||isExpense(a))&&year!=="all"&&year!=="custom";const tO=nom?0:a.opening,tC=tO-a.debit+a.credit;return{...a,tO,tC}}).filter(a=>Math.abs(a.tO)>tol||a.debit>tol||a.credit>tol||Math.abs(a.tC)>tol);const tODr=tR.reduce((s,a)=>s+(a.tO<0?-a.tO:0),0),tOCr=tR.reduce((s,a)=>s+(a.tO>0?a.tO:0),0),tCDr=tR.reduce((s,a)=>s+(a.tC<0?-a.tC:0),0),tCCr=tR.reduce((s,a)=>s+(a.tC>0?a.tC:0),0);return <table><thead><tr><SortHead field="name">Ledger</SortHead><SortHead field="opening" right>Opening Dr</SortHead><SortHead field="opening" right>Opening Cr</SortHead><SortHead field="debit" right>Period Dr</SortHead><SortHead field="credit" right>Period Cr</SortHead><SortHead field="closing" right>Closing Dr</SortHead><SortHead field="closing" right>Closing Cr</SortHead></tr></thead><tbody>{tR.map(a=><tr key={a.id}><td><LedgerLink a={a}/></td><td className="right">{a.tO<0?fmt(-a.tO):"-"}</td><td className="right">{a.tO>0?fmt(a.tO):"-"}</td><td className="right">{a.debit?fmt(a.debit):"-"}</td><td className="right">{a.credit?fmt(a.credit):"-"}</td><td className="right">{a.tC<0?fmt(-a.tC):"-"}</td><td className="right">{a.tC>0?fmt(a.tC):"-"}</td></tr>)}</tbody><tfoot><tr><th>Totals</th><th className="right">{fmt(tODr)}</th><th className="right">{fmt(tOCr)}</th><th className="right">{fmt(tR.reduce((s,a)=>s+a.debit,0))}</th><th className="right">{fmt(tR.reduce((s,a)=>s+a.credit,0))}</th><th className="right">{fmt(tCDr)}</th><th className="right">{fmt(tCCr)}</th></tr></tfoot></table>})()}</div>}{report==="income"&&<><GroupedReport title1="Expenditure" rows1={periodExpenseRows} title2="Income" rows2={periodIncomeRows} link={a=><LedgerLink a={a}/>} fmt={fmt}/><div className="period-result"><span>Period income</span><strong>{fmt(periodIncome)}</strong><span>Period expenditure</span><strong>{fmt(periodExpense)}</strong><span>Surplus / (Deficit) transferred to Capital</span><strong>{fmt(periodSurplus)}</strong></div></>} {report==="balance"&&<BalanceSheetReport assets={assetRows} liabilities={liabilityRows} capitalTransfer={capitalTransfer} plRows={rows.filter(isProfitLoss)} tol={tol} link={a=><LedgerLink a={a}/>} fmt={fmt}/>} {report==="cashflow"&&<div className="data-panel"><h3>Cash Flow - {periodLabel}</h3><div className="cash-flow-summary"><div className="report-line"><span>Opening cash and bank balance</span><strong>{fmt(cashOpening)}</strong></div></div><section className="report-group"><div className="group-heading"><strong>Cash inflows</strong><span>{fmt(cashInflows)}</span></div>{cashFlowGroups.filter(g=>g.inflow>tol).map(g=><div className="report-line" key={`in-${g.group}`}><button className="ledger-link" onClick={()=>setCashFlowDetail(g.group)}>{g.group}</button><strong>{fmt(g.inflow)}</strong></div>)}</section><section className="report-group"><div className="group-heading"><strong>Cash outflows</strong><span>{fmt(cashOutflows)}</span></div>{cashFlowGroups.filter(g=>g.outflow>tol).map(g=><div className="report-line" key={`out-${g.group}`}><button className="ledger-link" onClick={()=>setCashFlowDetail(g.group)}>{g.group}</button><strong>{fmt(g.outflow)}</strong></div>)}</section><div className="report-total"><span>Net increase / (decrease) in cash</span><strong>{fmt(cashNet)}</strong></div><div className="report-grand"><span>Closing cash and bank balance</span><strong>{fmt(cashFlowClosing)}</strong></div>{Math.abs(cashFlowClosing-cashBank)>.01&&<div className="balance-check difference"><strong>Reconciliation difference</strong><span>{fmt(cashFlowClosing-cashBank)}</span></div>}</div>}{report==="cash"&&<div className="data-panel"><h3>Cash and Bank Closing Balances</h3>{active.filter(a=>isCashBank(a)&&Math.abs(a.closing)>tol).map(a=><div className="report-line" key={a.id}><LedgerLink a={a}/><strong>{fmt(-a.closing)}</strong></div>)}<div className="report-total"><span>Total cash and bank</span><strong>{fmt(cashBank)}</strong></div></div>}</>}
{tab==="new"&&<div className="data-panel form-panel"><div className="form-heading"><h3>{editTx?"Edit posted voucher":copyTx?"Copy and edit voucher":"Record balanced voucher"}</h3>{(copyTx||editTx)&&<button onClick={()=>{setCopyTx(null);setEditTx(null)}}>Cancel</button>}</div>{editTx&&<p className="edit-note">You are changing posted voucher {editTx.type} {editTx.number}. Saving will replace this voucher and recalculate all affected reports.</p>}{copyTx&&<p className="copy-note">This is a new voucher based on {copyTx.type} {copyTx.number}. Change any field below; the original will not be changed.</p>}<form ref={entryFormRef} key={editTx?.guid||copyTx?.guid||"new"} className="entry-form" onSubmit={add}><label>Voucher type<select name="type" defaultValue={(editTx||copyTx)?.type||"Payment"}>{(data.voucherTypes||["Payment","Receipt","Contra","Journal"]).map(v=><option key={v}>{v}</option>)}</select></label><label>Date<input name="date" type="date" defaultValue={(editTx||copyTx)?.date||new Date().toISOString().slice(0,10)} required/></label><label>Amount<input name="amount" type="number" min="0.01" step="0.01" defaultValue={(editTx||copyTx)?Math.abs((editTx||copyTx)!.entries.find(e=>e.amount<0)?.amount||0):undefined} required/></label><label>Voucher number<input name="reference" value={editTx?.number||"Assigned automatically when saved"} readOnly aria-readonly="true"/></label><div className="inline-ledger-field"><label>Debit account<select name="debit" defaultValue={(editTx||copyTx)?.entries.find(e=>e.amount<0)?.accountId} required>{data.accounts.filter(a=>a.active!==false).map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><button type="button" className="inline-ledger-button" onClick={()=>setInlineLedgerSide("debit")}>+ New ledger</button></div><div className="inline-ledger-field"><label>Credit account<select name="credit" defaultValue={(editTx||copyTx)?.entries.find(e=>e.amount>0)?.accountId} required>{data.accounts.filter(a=>a.active!==false).map(a=><option value={a.id} key={a.id}>{a.name}</option>)}</select></label><button type="button" className="inline-ledger-button" onClick={()=>setInlineLedgerSide("credit")}>+ New ledger</button></div><label className="wide">Narration<textarea name="narration" rows={3} defaultValue={cleanText((editTx||copyTx)?.narration||"")}/></label><button className="primary wide">{editTx?"Encrypt and update voucher":copyTx?"Encrypt and save as new voucher":"Encrypt and save voucher"}</button></form>{inlineLedgerSide&&<div className="master-modal inline-ledger-modal"><form className="master-form" onSubmit={createLedgerInsideVoucher}><h3>Create Ledger from Voucher</h3><p>The ledger will be selected as the {inlineLedgerSide} account and synchronized to Tally before this voucher.</p><label>Ledger name<input name="name" required autoFocus/></label><label>Account group<select name="parent" required>{[...new Set([...data.accounts.map(a=>a.parent).filter(Boolean),(data.groups||[]).filter(g=>g.active!==false).map(g=>g.name)])].sort().map(group=><option key={group}>{group}</option>)}</select></label><label>Currency<select name="currency" defaultValue={data.currency}>{[...new Set([data.currency,...(data.currencies||[])])].map(currency=><option key={currency}>{currency}</option>)}</select></label><div className="opening-fields"><label>Opening balance<input name="opening" type="number" min="0" step="0.01" defaultValue="0"/></label><label>Balance side<select name="side" defaultValue="Dr"><option>Dr</option><option>Cr</option></select></label></div><label className="check-label"><input type="checkbox" checked readOnly/> Active ledger</label><div><button type="button" onClick={()=>setInlineLedgerSide(null)}>Cancel</button><button className="primary">Create and select</button></div></form></div>}</div>}
{cashFlowDetail&&<div className="drill-overlay" onClick={()=>setCashFlowDetail(null)}><div className="drill-panel" onClick={e=>e.stopPropagation()}><button className="drill-close" onClick={()=>setCashFlowDetail(null)}>Close</button><h2>{cashFlowDetail}</h2><p>Cash Flow | {periodLabel}</p><TransactionTable transactions={cashFlowItems.filter(x=>x.group===cashFlowDetail).slice().reverse().map(x=>x.t)} formatAmount={fmt} onView={t=>setSelectedVoucher(t as Tx)} onEdit={t=>editVoucher(t as Tx)} onCopy={t=>copyVoucher(t as Tx)} onDelete={t=>deleteVoucher(t as Tx)}/></div></div>}{selectedRow&&<div className="drill-overlay" onClick={()=>setSelected(null)}><div className="drill-panel" onClick={e=>e.stopPropagation()}><button className="drill-close" onClick={()=>setSelected(null)}>Close</button><h2>{selectedRow.name}</h2><p>{selectedRow.parent||selectedRow.category} | {periodLabel}</p><section className="drill-summary"><span>Opening<strong>{fmt(displayLedgerBalance(selectedRow,selectedRow.opening))}</strong></span><span>Period debit<strong>{fmt(selectedRow.debit)}</strong></span><span>Period credit<strong>{fmt(selectedRow.credit)}</strong></span><span>Closing<strong>{fmt(displayLedgerBalance(selectedRow,selectedRow.closing))}</strong></span></section><TransactionTable transactions={selectedTx} formatAmount={fmt} onView={t=>setSelectedVoucher(t as Tx)} onEdit={t=>editVoucher(t as Tx)} onCopy={t=>copyVoucher(t as Tx)} onDelete={t=>deleteVoucher(t as Tx)}/></div></div>}{selectedVoucher&&<div className="drill-overlay" onClick={()=>setSelectedVoucher(null)}><div className="drill-panel voucher-detail" onClick={e=>e.stopPropagation()}><button className="drill-close" onClick={()=>setSelectedVoucher(null)}>Close</button><h2>{selectedVoucher.type} Voucher {selectedVoucher.number}</h2><p>{selectedVoucher.date.split("-").reverse().join("-")} | {data.company}</p><section className="voucher-meta"><span>Date<strong>{selectedVoucher.date.split("-").reverse().join("-")}</strong></span><span>Voucher type<strong>{selectedVoucher.type}</strong></span><span>Reference<strong>{selectedVoucher.number||"-"}</strong></span><span>Status<strong>{selectedVoucher.cancelled?"Cancelled":"Posted"}</strong></span></section><h3>Narration</h3><p className="voucher-narration">{cleanText(selectedVoucher.narration)||"-"}</p><table><thead><tr><th>Ledger</th><th className="right">Debit</th><th className="right">Credit</th></tr></thead><tbody>{selectedVoucher.entries.map((e,i)=><tr key={`${e.accountId}-${i}`}><td>{e.accountName}</td><td className="right">{e.amount<0?fmt(-e.amount):"-"}</td><td className="right">{e.amount>0?fmt(e.amount):"-"}</td></tr>)}</tbody><tfoot><tr><th>Total</th><th className="right">{fmt(selectedVoucher.entries.filter(e=>e.amount<0).reduce((n,e)=>n-e.amount,0))}</th><th className="right">{fmt(selectedVoucher.entries.filter(e=>e.amount>0).reduce((n,e)=>n+e.amount,0))}</th></tr></tfoot></table><div className="voucher-detail-actions">{!selectedVoucher.cancelled&&<button className="edit-voucher" onClick={()=>{setSelectedVoucher(null);editVoucher(selectedVoucher)}}>Edit</button>}<button className="copy-voucher" onClick={()=>{setSelectedVoucher(null);copyVoucher(selectedVoucher)}}>Copy</button><button className="delete-voucher" onClick={()=>{setSelectedVoucher(null);deleteVoucher(selectedVoucher)}}>Delete</button></div></div></div>}</>}
function GroupedReport({title1,rows1,title2,rows2,link,fmt}:{title1:string;rows1:any[];title2:string;rows2:any[];link:(a:any)=>React.ReactNode;fmt:(n:number)=>string}){return <div className="report-grid"><GroupColumn title={title1} rows={rows1} link={link} fmt={fmt}/><GroupColumn title={title2} rows={rows2} link={link} fmt={fmt}/></div>}
function GroupColumn({title,rows,link,fmt}:{title:string;rows:any[];link:(a:any)=>React.ReactNode;fmt:(n:number)=>string}){const groups=new Map<string,any[]>();for(const row of rows){const key=row.parent||row.category||"Other";groups.set(key,[...(groups.get(key)||[]),row])}const sorted=[...groups.entries()].sort((a,b)=>a[0].localeCompare(b[0])),grand=rows.reduce((s,a)=>s+Math.abs(a.closing),0);return <div className="data-panel grouped-report"><h3>{title}</h3>{sorted.map(([group,items])=><section className="report-group" key={group}><div className="group-heading"><strong>{group}</strong><span>{fmt(items.reduce((s,a)=>s+Math.abs(a.closing),0))}</span></div>{items.sort((a,b)=>a.name.localeCompare(b.name)).map(a=><div className="report-line" key={a.id}>{link(a)}<strong>{fmt(Math.abs(a.closing))}</strong></div>)}</section>)}<div className="report-grand"><span>Total {title}</span><strong>{fmt(grand)}</strong></div></div>}function BalanceSheetReport({assets,liabilities,capitalTransfer,plRows,tol=.005,link,fmt}:{assets:any[];liabilities:any[];capitalTransfer:number;plRows:any[];tol?:number;link:(a:any)=>React.ReactNode;fmt:(n:number)=>string}){
  const [expanded,setExpanded]=React.useState<Set<string>>(new Set());
  const toggle=(k:string)=>setExpanded(p=>{const n=new Set(p);n.has(k)?n.delete(k):n.add(k);return n;});
  const BS:Record<string,string>={'capital account':'Capital Account','reserves & surplus':'Reserves & Surplus','secured loans':'Loans (Liability)','unsecured loans':'Loans (Liability)','bank od a/c':'Loans (Liability)','loans (liability)':'Loans (Liability)','sundry creditors':'Current Liabilities','duties & taxes':'Current Liabilities','provisions':'Current Liabilities','current liabilities':'Current Liabilities','suspense a/c':'Suspense A/c','fixed assets':'Fixed Assets','investments':'Investments','current assets':'Current Assets','cash-in-hand':'Current Assets','bank accounts':'Current Assets','loans & advances (asset)':'Current Assets','deposits (asset)':'Current Assets','sundry debtors':'Current Assets','miscellaneous expenditure (asset)':'Miscellaneous Expenditure'};
  const secFor=(a:any,def:string)=>{const g=(a.parent||a.category||'').toLowerCase().trim();return BS[g]||(g.includes('(asset)')?'Current Assets':def)};
  const L_ORD=['Capital Account','Reserves & Surplus','Loans (Liability)','Current Liabilities','Suspense A/c'];
  const R_ORD=['Fixed Assets','Investments','Current Assets','Miscellaneous Expenditure'];
  const SUB:Record<string,number>={'cash-in-hand':1,'bank accounts':2,'loans & advances (asset)':3,'deposits (asset)':4,'sundry debtors':5,'bank od a/c':1,'secured loans':2,'unsecured loans':3,'loans (liability)':4,'sundry creditors':1,'duties & taxes':2,'provisions':3,'current liabilities':4,'capital account':1,'reserves & surplus':2};
  const sOrd=(p:string)=>SUB[(p||'').toLowerCase().trim()]??99;
  const mkMap=(rows:any[],def:string)=>{const m=new Map<string,Map<string,any[]>>();for(const a of rows){const sec=secFor(a,def);if(!m.has(sec))m.set(sec,new Map());const pg=a.parent||a.category||'Other';const sm=m.get(sec)!;if(!sm.has(pg))sm.set(pg,[]);sm.get(pg)!.push(a)}return m};
  const lMap=mkMap(liabilities,'Current Liabilities');
  const rMap=mkMap(assets,'Current Assets');
  const plClose=plRows.reduce((s:number,a:any)=>s+a.closing,0);
  const PLdisp=-(plClose+capitalTransfer);
  const plOpen=plRows.reduce((s:number,a:any)=>s+a.opening,0);
  const assetLedg=assets.reduce((s:number,a:any)=>s-a.closing,0);
  const liabLedg=liabilities.reduce((s:number,a:any)=>s+a.closing,0);
  const assetSide=assetLedg+(PLdisp>=0?PLdisp:0);
  const liabSide=liabLedg+(PLdisp<0?-PLdisp:0);
  const diff=assetSide-liabSide;
  const showPL=Math.abs(PLdisp)>tol||plRows.some((a:any)=>Math.abs(a.closing)>tol);
  const renderSec=(sk:string,side:'L'|'R',sub:Map<string,any[]>)=>{
    const sign=side==='L'?1:-1;
    const tot=[...sub.values()].flat().reduce((s,a)=>s+sign*a.closing,0);
    const isE=expanded.has(sk);
    return <section key={sk} className="bs-sec">
      <button className="bs-sec-head" onClick={()=>toggle(sk)}>
        <span className="bs-arr">{isE?'▼':'▶'}</span>
        <strong>{sk}</strong>
        <span className="bs-amt">{fmt(tot)}</span>
      </button>
      {isE&&<div className="bs-body">
        {[...sub.entries()].sort((a,b)=>sOrd(a[0])-sOrd(b[0])).map(([grp,rows])=>{
          const gt=rows.reduce((s,a)=>s+sign*a.closing,0);
          return <div key={grp} className="bs-sg">
            {sub.size>1&&<div className="bs-sg-head"><em>{grp}</em><span>{fmt(gt)}</span></div>}
            {rows.slice().sort((a:any,b:any)=>a.name.localeCompare(b.name)).map((a:any)=><div key={a.id} className="bs-row">{link(a)}<span>{fmt(sign*a.closing)}</span></div>)}
          </div>;
        })}
      </div>}
    </section>;
  };
  const renderPL=(side:'L'|'R')=>{
    const isE=expanded.has('__pl__');
    const lbl=side==='R'&&PLdisp>0?'Profit & Loss A/c (Deficit)':side==='L'?'Profit & Loss A/c (Surplus)':'Profit & Loss A/c';
    return <section className="bs-sec bs-pl-sec">
      <button className="bs-sec-head" onClick={()=>toggle('__pl__')}>
        <span className="bs-arr">{isE?'▼':'▶'}</span>
        <strong>{lbl}</strong>
        <span className="bs-amt">{fmt(Math.abs(PLdisp))}</span>
      </button>
      {isE&&<div className="bs-body">
        {Math.abs(plOpen)>tol&&<div className="bs-row"><span>Opening balance b/f</span><span>{fmt(Math.abs(plOpen))}</span></div>}
        <div className="bs-row"><span>{capitalTransfer>=0?'Current period surplus':'Current period deficit'}</span><span>{fmt(Math.abs(capitalTransfer))}</span></div>
        {Math.abs(plClose-plOpen)>tol&&<div className="bs-row"><span>Transfers during period</span><span>{fmt(Math.abs(plClose-plOpen))}</span></div>}
      </div>}
    </section>;
  };
  return <>
    <div className="bs-grid">
      <div className="bs-col">
        <div className="bs-col-head">Liabilities</div>
        {L_ORD.filter(s=>lMap.has(s)&&[...lMap.get(s)!.values()].flat().length>0).map(s=>renderSec(s,'L',lMap.get(s)!))}
        {showPL&&PLdisp<0&&renderPL('L')}
        <div className="bs-total"><span>Total Capital and Liabilities</span><strong>{fmt(liabSide)}</strong></div>
      </div>
      <div className="bs-col">
        <div className="bs-col-head">Assets</div>
        {R_ORD.filter(s=>rMap.has(s)&&[...rMap.get(s)!.values()].flat().length>0).map(s=>renderSec(s,'R',rMap.get(s)!))}
        {showPL&&PLdisp>=0&&renderPL('R')}
        <div className="bs-total"><span>Total Assets</span><strong>{fmt(assetSide)}</strong></div>
      </div>
    </div>
    <div className={`balance-check ${Math.abs(diff)<0.01?"tied":"difference"}`}>
      <strong>Balance Sheet check</strong><span>{fmt(Math.abs(diff))}</span>
      <small>{Math.abs(diff)<0.01?'Balanced':'Difference: review ledger classifications or opening balances'}</small>
    </div>
  </>;
}






